import { SortedAndFilteredHistoryPipe } from './sorted-and-filtered-history.pipe';

describe('SortedAndFilteredHistoryPipe', () => {
  it('create an instance', () => {
    const pipe = new SortedAndFilteredHistoryPipe();
    expect(pipe).toBeTruthy();
  });
});
