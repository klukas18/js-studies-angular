import { FilteredHistoryPipe } from './filtered-history.pipe';

describe('FilteredHistoryPipe', () => {
  it('create an instance', () => {
    const pipe = new FilteredHistoryPipe();
    expect(pipe).toBeTruthy();
  });
});
